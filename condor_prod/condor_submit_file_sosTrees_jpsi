notification          = Complete
notify_user	      = panos.katsoulis@cern.ch
executable            = sosTrees_production.sh
getenv                = True
arguments             = "--condor $(process) $(ClusterId) $(ProcId) /eos/user/k/kpanos/sostrees/2016_80X run_susyMultilepton_cfg_jpsi_noMetCut 300000 2>&1"
output                = output/hello.$(ClusterId).$(ProcId).out
error                 = error/hello.$(ClusterId).$(ProcId).err
log                   = log/hello.$(ClusterId).$(ProcId).log
RequestCpus           = 4
max_retries           = 3

+JobFlavour = "testmatch"
queue process from (
  ZToJPsiMuMu
  JPsiToMuMu
)

